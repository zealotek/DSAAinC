#include"list.h"

int insert_list(struct list *my_list, char element, int position)
{
	//position out of list length
	//position is negitive	
	if(position < 0)
	{
		printf("position is negitive\n");
		return -1;
	}

}
